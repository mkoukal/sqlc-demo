version: "2"
sql:
  - engine: "postgresql"
    queries: "./queries"
    schema: "schema/"
    gen:
      go:
        package: "db"
        sql_package: "pgx/v5"
        out: "../db"
        emit_json_tags: true
